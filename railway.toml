[build]
builder = "nixpacks"
buildCommand = "cd backend && npm install && npm run build"

[deploy]
startCommand = "cd backend && npm run start"
healthcheckPath = "/" # Ajusta esto a tu ruta de healthcheck
restartPolicyType = "on_failure"

[service]
dirPath = "backend"