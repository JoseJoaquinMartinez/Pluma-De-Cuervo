export interface SingUpInterface {
  label: string;
  type: string;
  name: string;
  placeHolder: string;
}

export interface UnderlineText {
  text: string;
  link: string;
}
export interface NewUserInterface {
  authToke: string;
  message: string;
}